export type Pose={x:number;y:number;yaw:number};
export type VocalState={transcript:string;acoustics:{rmsDb:number;peakDb:number;pitchVariation:number;pauseRatio:number;voicedMs:number}|null;expressions:{name:string;score:number}[];derived:{urgency:number;hesitation:number;calmness:number};timestamp:number};
export type BehaviorPolicy={priority:number;maxSpeed:number;approachSpeed:number;personalSpaceMeters:number;requireConfirmation:boolean;responseVerbosity:'minimal'|'normal';interruptThreshold:number};
export type RobotState={sceneObjects?:{name:string;x:number;y:number;halfX:number;halfY:number}[];pose:Pose;velocity:number;commandedSpeed:number;qpos:number[];currentWaypoint?:string;status:'idle'|'moving'|'stopped'|'error';timestamp:number;simTime:number;backend:string;estop:boolean;reason:string;height:number;contacts:number;nearestObstacle:number;motion?:'turn'|'drive'|'walk'|'hold'};
export type ProviderMode='LIVE'|'DEMO'|'LOCAL'|'OFFLINE'|'READY';
export type Trace={id:string;at:number;component:string;message:string;durationMs?:number};
export type MemoryEvent={id:string;object:string;location:string;waypoint:string;timestamp:number;evidence:string;source:'DEMO'|'LIVE'|'LOCAL';videoNo?:string;raw?:unknown};
export type Metric={name:string;ms:number|null;basis:string};
export type ToolCall={name:string;arguments:Record<string,unknown>};
export type Snapshot={inferenceProvider?:string|null;conversation?:{role:'user'|'assistant';content:string}[];conversationStatus?:'idle'|'thinking';inputSource:'MICROPHONE'|'FIXTURE'|'TYPED'|'KEYBOARD';mode:'REAL'|'DEMO';phase:string;robot:RobotState|null;vocal:VocalState|null;policy:BehaviorPolicy;traces:Trace[];metrics:Metric[];providers:Record<string,{mode:ProviderMode;active:boolean}>;plan:ToolCall[];memory:MemoryEvent|null;response:string;reflex:string;error:string|null;pendingConfirmation:boolean;history:{label:string;speed:number;priority:number;space:number}[]};
